--- app/src/main/java/com/example/ui/HomeDashboardScreen.kt
+++ app/src/main/java/com/example/ui/HomeDashboardScreen.kt
@@ -23,8 +23,9 @@
 @Composable
 fun HomeDashboardScreen(viewModel: PDFViewModel, onNavigate: (Int) -> Unit, onAction: (String) -> Unit) {
+    val history = viewModel.historyList.collectAsState().value
     Column(
         modifier = Modifier
             .fillMaxSize()
             .verticalScroll(rememberScrollState())
@@ -38,9 +39,9 @@
             horizontalArrangement = Arrangement.spacedBy(12.dp)
         ) {
-            StatCard(modifier = Modifier.weight(1f), "PDFs Created", "14", Icons.Default.PictureAsPdf, Color(0xFFEF4444))
-            StatCard(modifier = Modifier.weight(1f), "Pages Scanned", "48", Icons.Default.Scanner, Color(0xFF10B981))
-            StatCard(modifier = Modifier.weight(1f), "AI Requests", "128", Icons.Default.AutoAwesome, Color(0xFF3B82F6))
+            StatCard(modifier = Modifier.weight(1f), "Total PDFs", history.size.toString(), Icons.Default.PictureAsPdf, Color(0xFFEF4444))
+            StatCard(modifier = Modifier.weight(1f), "Total Pages", history.sumOf { it.numPages }.toString(), Icons.Default.Scanner, Color(0xFF10B981))
+            StatCard(modifier = Modifier.weight(1f), "Favorites", history.count { it.isFavorite }.toString(), Icons.Default.AutoAwesome, Color(0xFF3B82F6))
         }
 
@@ -71,9 +72,13 @@
                 Text("See All", fontSize = 12.sp, color = Color(0xFF3B82F6), modifier = Modifier.clickable { onNavigate(3) })
             }
             Spacer(modifier = Modifier.height(12.dp))
             
-            // Dummy List
-            repeat(3) {
-                RecentFileItem(name = "Document_${it}.pdf", date = "Just now", size = "${it + 1}.2 MB")
+            if (history.isEmpty()) {
+                 Text("No recent files", color = Color.Gray, modifier = Modifier.padding(16.dp))
+            } else {
+                 history.take(3).forEach { pdf ->
+                     RecentFileItem(name = pdf.displayName, date = pdf.dateString, size = pdf.fileSizeFormatted)
+                 }
             }
         }
